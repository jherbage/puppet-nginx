# File managed by puppet

server {
  listen <%= @port %>;
  root <%= @vhost_docroot %>;
  server_name <%= @name %> <%= @server_aliases %>;

  access_log <%= @log_dir %>/<%= @name %>.access.log;
  error_log <%= @log_dir %>/<%= @name %>.error.log;
}
